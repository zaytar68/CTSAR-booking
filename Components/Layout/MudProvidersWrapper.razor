@*
====================================================================
MudProvidersWrapper.razor : Wrapper pour les MudBlazor Providers
====================================================================
Ce composant entoure tout le contenu avec les providers MudBlazor.
Note: Ce composant doit rester SANS @rendermode pour pouvoir
recevoir du ChildContent depuis Routes.razor.
*@

@inject ThemeService ThemeService

<MudThemeProvider @rendermode="InteractiveServer" IsDarkMode="@ThemeService.IsDarkMode" Theme="_theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

@ChildContent

@code {
    private MudTheme _theme = new MudTheme();

    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}
